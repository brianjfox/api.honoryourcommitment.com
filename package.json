{
  "name": "api-honoryourcommitment",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API server for the Portugal Must Honor Its Commitments campaign — petition signatures, case registry, legal claimants.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node migrations/run.js",
    "smoke": "node scripts/smoke.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0",
    "pino-pretty": "^11.2.2"
  }
}
